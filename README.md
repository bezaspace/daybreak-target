# daybreak-target
Scratch target repo for Daybreak Phase 1 sandbox tests

## Usage

Install dependencies and run the tests:

```bash
npm install
npm test
```

### sum

The `sum` function adds two numbers together:

```js
import { sum } from "./sum.js";

console.log(sum(2, 3)); // 5
```
